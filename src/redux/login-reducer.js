const inititalState ={
    isCodeCame : true 
}



const loginReducer = (state = inititalState, action)=>{
    switch(action.type){
        default:
            return state
    }
}

export default loginReducer

